{
  "name": "smitetimerapi",
  "version": "1.0.0",
  "engines": {
    "node": "12.19.0"
  },
  "description": "smite relic timer api",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node Server.js",
    "heroku-postbuild": "npm install --prefix client && npm run build --prefix client"
  },
  "author": "nick allen",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "mongoose": "^5.10.9"
  }
}
